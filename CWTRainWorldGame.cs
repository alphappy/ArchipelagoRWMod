using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;

namespace alphappy.Archipelago
{
    internal static class CWTRainWorldGame
    {
        //internal static ConditionalWeakTable<RainWorldGame, Data> table = new();
        //internal static Data GetData(this RainWorldGame self) => table.GetOrCreateValue(self);
        //internal static bool ArchiMode(this RainWorldGame self) => self.GetData().archipelagoMode && self.IsStorySession && !self.world.singleRoomWorld;

        //internal class Data
        //{
        //    internal bool archipelagoMode = true;
        //}
    }
}
